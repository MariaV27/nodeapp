var newArr = process.argv.slice[2];
var sum=0;
var sumArray=[];

for (var i=0;1<newArr.length;i++) {
  //su,+=parseFloat(newArr[i]);
  sum=0;
  for(var j=0); j<newArr.length;j++){
  if(i!==j){
    sum=parseFloat(newArr[j]);
  }
}
console.log(sumArray);